{
  "name": "@ai-lint/web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "predev": "cargo build --locked --manifest-path ../../Cargo.toml",
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "prepreview": "cargo build --locked --manifest-path ../../Cargo.toml",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^24.13.3",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.5",
    "@vitejs/plugin-react": "^6.1.1",
    "typescript": "~6.0.2",
    "vite": "^8.2.2"
  }
}
